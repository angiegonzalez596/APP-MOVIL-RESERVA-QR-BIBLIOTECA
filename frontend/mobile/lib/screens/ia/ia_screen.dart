import 'package:flutter/material.dart';

class IaScreen extends StatelessWidget {
  const IaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('IA')));
  }
}
